# Spec — qrspi-command-surface

> New capability introduced by the `kit-quality-hardening` change. Defines the
> command, agent, and skill surface of the QRSPI kit after the opsx removal,
> choreography DRY refactor, skill-load preamble lint, and tool-grant audit
> applied by this change.

## ADDED Requirements

### Requirement: Opsx commands removed from the kit
The system MUST NOT include the 5 opsx command files
(`claude/commands/opsx/propose.md`, `claude/commands/opsx/explore.md`,
`claude/commands/opsx/apply.md`, `claude/commands/opsx/archive.md`,
`claude/commands/opsx/sync.md`). These files SHALL be deleted from the repo and
SHALL NOT be re-generated by the Copilot sync generator.

#### Scenario: fresh sync produces no opsx prompt files
- **WHEN** `node sync-copilot.mjs` is run after the opsx commands are deleted
- **THEN** no `copilot/prompts/opsx-*.prompt.md` files appear in the generated
  output.

#### Scenario: plugin.json no longer advertises opsx commands
- **WHEN** the kit is installed via the Claude Code marketplace
- **THEN** no `/opsx:*` commands are registered in the user's Claude Code
  command palette.

### Requirement: Orphaned OpenSpec-generated skills removed
The system MUST NOT include the 3 orphaned OpenSpec-generated skills
(`claude/skills/openspec-propose/`, `claude/skills/openspec-explore/`,
`claude/skills/openspec-apply-change/`). These directories SHALL be deleted from
the repo. The 3 load-bearing skills (`openspec-workflow`,
`openspec-archive-change`, `openspec-sync-specs`) MUST be retained.

#### Scenario: sync produces no instructions for deleted skills
- **WHEN** `node sync-copilot.mjs` is run after the orphaned skill directories
  are deleted
- **THEN** no `copilot/instructions/openspec-propose.instructions.md`,
  `openspec-explore.instructions.md`, or
  `openspec-apply-change.instructions.md` files appear in the generated output.

#### Scenario: load-bearing skills still available
- **WHEN** a QRSPI stage agent loads `openspec-workflow`, `openspec-archive-change`,
  or `openspec-sync-specs`
- **THEN** the corresponding `claude/skills/<skill>/SKILL.md` file is present
  and loads successfully.

### Requirement: Install scripts self-heal stale Copilot files
The `install.ps1` and `install.sh` scripts MUST explicitly delete the 8
now-stale Copilot files from the user's `~/.copilot/` directory before
copying the new artifacts, so that prior-install users do not retain orphaned
files. The 8 targets are: `~/.copilot/prompts/opsx-propose.prompt.md`,
`opsx-explore.prompt.md`, `opsx-apply.prompt.md`, `opsx-archive.prompt.md`,
`opsx-sync.prompt.md`; and
`~/.copilot/instructions/openspec-propose.instructions.md`,
`openspec-explore.instructions.md`, `openspec-apply-change.instructions.md`.

#### Scenario: user re-runs install after upgrading the kit
- **WHEN** a user who had a `0.1.0` install re-runs `./install.ps1` (or
  `./install.sh`) after pulling the `0.2.0` kit
- **THEN** all 8 stale files are removed from `~/.copilot/` before the new
  files are copied in, leaving no orphaned opsx or openspec-{propose,explore,
  apply-change} artifacts.

#### Scenario: clean install does not fail on missing stale files
- **WHEN** a user runs the install script on a machine that never had the
  `0.1.0` opsx files
- **THEN** the sweep attempts to delete the 8 target paths but does not error
  on files that do not exist (silent no-op for absent targets).

### Requirement: Choreography procedure canonical in qrspi-workflow skill
The `qrspi-workflow` skill body MUST contain the canonical descriptions of the
commit step, next-stage handoff, and Glob-based precondition pattern. Each QRSPI
stage command MUST keep only a thin inline stub naming its own artifact filename,
commit message template, and next-stage command, and MUST reference the
`qrspi-workflow` skill for the invariant procedure text.

#### Scenario: contributor reads a stage command
- **WHEN** a contributor opens any stage command file (e.g., `research.md`)
- **THEN** the command contains a thin stub with the stage-specific variable
  parts (artifact name, commit message, next command) and a reference to
  `qrspi-workflow` for the commit-step and handoff procedure, rather than a
  full verbatim copy of the procedure.

#### Scenario: choreography procedure updated in one place
- **WHEN** the commit-step wording is updated in the `qrspi-workflow` skill
- **THEN** all stage commands automatically reflect the updated procedure
  because they reference the skill rather than duplicating its text.

### Requirement: Skill-load preamble references enforced by lint
All `Load skill X` references in agent body text MUST resolve to a real
`claude/skills/<X>/SKILL.md`. This constraint MUST be enforced by the CI lint
job so that a dangling skill reference is caught before merge.

#### Scenario: new agent added with a typo in a skill name
- **WHEN** a contributor adds an agent that references `Load skill qrspi-workflw`
  (typo) and the CI lint job runs
- **THEN** the lint reports the unresolved skill name and the job fails.

### Requirement: Edit removed from researcher and planner frontmatter
Agents that only write fresh artifacts and never edit existing files MUST NOT
carry the `Edit` tool grant. Specifically, `qrspi-researcher.md` and
`qrspi-planner.md` MUST have `Edit` removed from their `tools:` frontmatter.
`qrspi-questioner.md` MUST RETAIN `Edit`: its mandatory backlog step flips the
`openspec/backlog.md` row in place (an edit to an existing file), so `Edit` is
load-bearing there, not vestigial.

The Copilot-generated equivalents of the researcher and planner are NOT required
to lose `edit/editFiles`: the generator's `mapTools` collapses both Claude
`Write` and `Edit` onto the single Copilot `edit/editFiles` grant, and these
agents legitimately retain `Write` to author their artifact. Copilot offers no
create-only grant, so stripping `edit/editFiles` would remove their ability to
write at all. The least-privilege tightening is therefore a Claude-side concern.

#### Scenario: researcher and planner frontmatter is checked
- **WHEN** `qrspi-researcher.md` and `qrspi-planner.md` frontmatter is inspected
- **THEN** each `tools:` field contains `Read, Write, Bash, Glob, Grep, Skill`
  but does NOT contain `Edit`.

#### Scenario: questioner retains Edit for the in-place backlog flip
- **WHEN** `qrspi-questioner.md` frontmatter is inspected
- **THEN** `tools:` still contains `Edit`.

### Requirement: Agent tool grants for designer, architect, and implementer retained
The `Agent` grant MUST be retained in the `tools:` frontmatter of
`qrspi-designer.md`, `qrspi-architect.md`, and `qrspi-implementer.md`. These
agents support human-directed ad-hoc expert-subagent delegation that is
intentional even when not scripted in the body instructions.

#### Scenario: architect agent retains Agent grant
- **WHEN** `qrspi-architect.md` frontmatter is inspected
- **THEN** `tools:` includes `Agent`.
